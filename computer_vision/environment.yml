# To activate the conda environment, run:
# conda env create -f environment.yml
# conda activate basketbot-env


name: basketbot-env
channels:
  - defaults
  - conda-forge
  - robostack
dependencies:
  - python=3.10
  - numpy=1.21.5
  - opencv-contrib-python==4.7.0.72
  - pyrealsense2=2.54.2.5684  # Install via conda
  - pip:
      - ultralytics==8.3.9

